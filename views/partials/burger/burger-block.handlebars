<li>
    {{name}}

    <button class="change-devour" data-id="{{id}}" data-newburger="{{devour}}">
        {{#if devour}}Burp!{{else}}Eat!{{/if}}
    </button>
    <button class='delete-burger' data-id='{{this.id}}'>Burp!</button>
</li>